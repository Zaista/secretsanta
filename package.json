{
    "name": "secretsanta",
    "scripts": {
        "test": "cypress open --e2e --browser chrome",
        "run": "cypress run --browser chrome",
        "test_ci": "cy2 run --record --key XXX --parallel --ci-build-id $BUILD --config baseUrl=https://secretsanta.jovanilic.com"
    },
    "devDependencies": {
        "@deploysentinel/cypress-debugger": "^0.4.11",
        "cy2": "2.0.1",
        "cypress": "10.9.0"
    }
}
